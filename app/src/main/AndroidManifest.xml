<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.advinity.carbonteam.hydrocarbon">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_hc_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.DrawerActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.AboutUsActivity"
            android:label="@string/activity_title_about_us" />
        <activity
            android:name=".activity.SettingsActivity"
            android:label="@string/activity_title_settings" />
        <activity
            android:name=".activity.DescriptionActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.EbookDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.QuizDetailActivity"
            android:label="@string/activity_title_quiz" />
        <activity
            android:name=".activity.ResultActivity"
            android:label="@string/activity_title_result"/>
    </application>

</manifest>